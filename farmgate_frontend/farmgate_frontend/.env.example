# Copy to .env.development or .env.production and fill in values.
# Never commit real credentials — .env* files are gitignored.

VITE_API_URL=http://localhost:8000/api

# Google OAuth Web client ID (public identifier; configure in Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=
